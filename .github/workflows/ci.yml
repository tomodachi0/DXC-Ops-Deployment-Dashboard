name: CI

on:
  push:
    branches: [dev]

jobs:
  all:
    runs-on: ubuntu-latest
    permissions:
      contents: write     
      packages: write 
    defaults:
      run:
        working-directory: backend

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install ruff pytest httpx

      - name: Lint code
        run: ruff check --output-format=github .

      - name: Run tests
        run: pytest

      - name: Build backend image
        working-directory: .
        run: docker build -t dxc-backend:ci-test ./backend

      - name: Build frontend image
        working-directory: .
        run: docker build -t dxc-frontend:ci-test ./frontend

      - name: Update image tag in manifest
        working-directory: .
        run: |
          sed -i "s|image: ghcr.io/.*/backend:.*|image: ghcr.io/${{ github.repository }}/backend:${{ env.SHA_SHORT }}|" k8s/backend/deployment.yaml
          sed -i "s|image: ghcr.io/.*/frontend:.*|image: ghcr.io/${{ github.repository }}/frontend:${{ env.SHA_SHORT }}|" k8s/frontend/deployment.yaml
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add k8s/backend/deployment.yaml k8s/frontend/deployment.yaml
          git commit -m "Deploy commit ${{ env.SHA_SHORT }}" || echo "No changes to commit"
          git push
          